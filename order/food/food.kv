<FoodOrderPopUp>:
    orientation: 'vertical'
    BoxLayout:
        Label:
            text: 'Factor id'
        Spinner:
            id: factor_id_selector
            text: 'Choose a factor'
            values: root.pick_factor()
            on_text: root.update_form()
        Button:
            id: new_factor
            text: 'new'
            on_release: root.new()

    BoxLayout:
        Label:
            text: 'Customer'
        Spinner:
            id: customer_selector
            text: 'Choose a factor'
            disabled: True
        Button:
            id: set_customer
            text: 'set customer'
            on_release: root.set_food_customer()
            disabled: True

    BoxLayout:
        Label:
            text: 'Food'
        Spinner:
            id: food_selector
            text: 'Choose Food'
            values: root.pick_food()

    BoxLayout:
        Label:
            text: 'Address'
        Spinner:
            id: address_selector
            text: 'Restaurant'
            disabled: True

    BoxLayout:
        Label:
            text: 'Biker'
        Spinner:
            id: biker_selector
            text: 'Choose Someone'
            disabled: True

    BoxLayout:
        Button:
            id: submit
            text: 'Submit'
            on_release: root.submit()

        Button:
            id: cancel
            text: 'Cancel'